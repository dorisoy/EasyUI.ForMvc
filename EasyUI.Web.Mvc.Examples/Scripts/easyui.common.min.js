!function (a, b, c) {
    "use strict";

    function d(a) {
        var c;
        return c = y(a), c !== c ? 0 : 0 === c || b === c || -b === c ? c : (c > 0 || -1) * x.floor(x.abs(c))
    }

    function e(a, b) {
        var e, f, g, h, i;
        for (f = w(this), g = f.length >>> 0, c !== a ? (a = d(a), h = 0 > a ? x.max(g + a, 0) : x.min(a, g)) : h = 0, c !== b && (b = d(b), g = 0 > b ? x.max(g + b, 0) : x.min(b, g)), e = new v(g - h), i = 0; g > h; ++h, ++i) h in f && (e[i] = f[h]);
        return e
    }

    function f(a, b) {
        var e, f, g;
        for (e = w(this), f = e.length >>> 0, c !== b ? (b = d(b), g = 0 > b ? x.max(f + b, 0) : x.min(b, f)) : g = 0; f > g; ++g) if (g in e && a === e[g]) return g;
        return -1
    }

    function g(a, b) {
        var e, f, g;
        for (e = w(this), f = e.length >>> 0, c !== b ? (b = d(b), g = 0 > b ? f - x.abs(b) : x.min(b, f - 1)) : g = f - 1; g > -1; --g) if (g in e && a === e[g]) return g;
        return -1
    }

    function h(a, b) {
        var c, d, e;
        for (c = w(this), p(a), d = c.length >>> 0, e = 0; d > e; ++e) e in c && a.call(b, c[e], e, c)
    }

    function i(a, b) {
        var c, d, e;
        for (c = w(this), p(a), d = c.length >>> 0, e = 0; d > e; ++e) if (e in c && !a.call(b, c[e], e, c)) return !1;
        return !0
    }

    function j(a, b) {
        var c, d, e;
        for (c = w(this), p(a), d = c.length >>> 0, e = 0; d > e; ++e) if (e in c && a.call(b, c[e], e, c)) return !0;
        return !1
    }

    function k(a, b) {
        var c, d, e, f, g = [];
        for (c = w(this), p(a), d = c.length >>> 0, e = f = 0; d > e; ++e) e in c && a.call(b, c[e], e, c) && (g[f++] = c[e]);
        return g
    }

    function l(a, b) {
        var c, d, e, f = [];
        for (c = w(this), p(a), d = c.length >>> 0, e = 0; d > e; ++e) e in c && (f[e] = a.call(b, c[e], e, c));
        return f
    }

    function m(a, b) {
        var d, e, f, g;
        for (d = w(this), p(a), e = c !== b, f = d.length >>> 0, g = 0; f > g; ++g) g in d && (e ? b = a(b, d[g], g, d) : (b = d[g], e = !0));
        return q(e), b
    }

    function n(a, b) {
        var d, e, f;
        for (d = w(this), p(a), e = c !== b, f = (d.length >>> 0) - 1; f > -1; --f) f in d && (e ? b = a(b, d[f], f, d) : (b = d[f], e = !0));
        return q(e), b
    }

    function o(a) {
        return "[object Array]" === w.prototype.toString.call(a)
    }

    function p(a) {
        if ("[object Function]" !== w.prototype.toString.call(a)) throw new Error(a + " is not a function")
    }

    function q(a) {
        if (!a) throw new Error("reduce of empty array with no initial value")
    }

    function r(a) {
        var b = !0;
        if (v.prototype[a]) try {
            v.prototype[a].call(c, /test/, null), b = !1
        } catch (d) { } else b = !1;
        return b
    }

    function s(a) {
        var b = !0;
        if (v[a]) try {
            v[a](c, /test/, null), b = !1
        } catch (d) { } else b = !1;
        return b
    }

    function t(a) {
        s(a) || (v[a] = u(a))
    }

    function u(a) {
        return function (b) {
            var d;
            if (c === b || null === b) throw new Error("Array.prototype." + a + " called on " + b);
            return d = v.prototype.slice.call(arguments, 1), v.prototype[a].apply(b, d)
        }
    }
    var v = a.Array,
		w = a.Object,
		x = a.Math,
		y = a.Number,
		z = {
		    indexOf: f,
		    lastIndexOf: g,
		    forEach: h,
		    every: i,
		    some: j,
		    filter: k,
		    map: l,
		    reduce: m,
		    reduceRight: n
		};
    for (var A in z) z.hasOwnProperty(A) && (r(A) || (v.prototype[A] = z[A]), t(A));
    if (v.isArray = v.isArray || o, ["concat", "join", "slice", "pop", "push", "reverse", "shift", "sort", "splice", "unshift"].forEach(t), document) try {
        v.slice(document.childNodes)
    } catch (B) {
        v.prototype.slice = e
    }
}(this, 1 / 0),

jQuery.easing.jswing = jQuery.easing.swing, jQuery.extend(jQuery.easing, {
    def: "easeOutQuad",
    swing: function (a, b, c, d, e) {
        "use strict";
        return jQuery.easing[jQuery.easing.def](a, b, c, d, e)
    },
    easeInQuad: function (a, b, c, d, e) {
        return d * (b /= e) * b + c
    },
    easeOutQuad: function (a, b, c, d, e) {
        return -d * (b /= e) * (b - 2) + c
    },
    easeInOutQuad: function (a, b, c, d, e) {
        return (b /= e / 2) < 1 ? d / 2 * b * b + c : -d / 2 * (--b * (b - 2) - 1) + c
    },
    easeInCubic: function (a, b, c, d, e) {
        return d * (b /= e) * b * b + c
    },
    easeOutCubic: function (a, b, c, d, e) {
        return d * ((b = b / e - 1) * b * b + 1) + c
    },
    easeInOutCubic: function (a, b, c, d, e) {
        return (b /= e / 2) < 1 ? d / 2 * b * b * b + c : d / 2 * ((b -= 2) * b * b + 2) + c
    },
    easeInQuart: function (a, b, c, d, e) {
        return d * (b /= e) * b * b * b + c
    },
    easeOutQuart: function (a, b, c, d, e) {
        return -d * ((b = b / e - 1) * b * b * b - 1) + c
    },
    easeInOutQuart: function (a, b, c, d, e) {
        return (b /= e / 2) < 1 ? d / 2 * b * b * b * b + c : -d / 2 * ((b -= 2) * b * b * b - 2) + c
    },
    easeInQuint: function (a, b, c, d, e) {
        return d * (b /= e) * b * b * b * b + c
    },
    easeOutQuint: function (a, b, c, d, e) {
        return d * ((b = b / e - 1) * b * b * b * b + 1) + c
    },
    easeInOutQuint: function (a, b, c, d, e) {
        return (b /= e / 2) < 1 ? d / 2 * b * b * b * b * b + c : d / 2 * ((b -= 2) * b * b * b * b + 2) + c
    },
    easeInSine: function (a, b, c, d, e) {
        return -d * Math.cos(b / e * (Math.PI / 2)) + d + c
    },
    easeOutSine: function (a, b, c, d, e) {
        return d * Math.sin(b / e * (Math.PI / 2)) + c
    },
    easeInOutSine: function (a, b, c, d, e) {
        return -d / 2 * (Math.cos(Math.PI * b / e) - 1) + c
    },
    easeInExpo: function (a, b, c, d, e) {
        return 0 == b ? c : d * Math.pow(2, 10 * (b / e - 1)) + c
    },
    easeOutExpo: function (a, b, c, d, e) {
        return b == e ? c + d : d * (-Math.pow(2, -10 * b / e) + 1) + c
    },
    easeInOutExpo: function (a, b, c, d, e) {
        return 0 == b ? c : b == e ? c + d : (b /= e / 2) < 1 ? d / 2 * Math.pow(2, 10 * (b - 1)) + c : d / 2 * (-Math.pow(2, -10 * --b) + 2) + c
    },
    easeInCirc: function (a, b, c, d, e) {
        return -d * (Math.sqrt(1 - (b /= e) * b) - 1) + c
    },
    easeOutCirc: function (a, b, c, d, e) {
        return d * Math.sqrt(1 - (b = b / e - 1) * b) + c
    },
    easeInOutCirc: function (a, b, c, d, e) {
        return (b /= e / 2) < 1 ? -d / 2 * (Math.sqrt(1 - b * b) - 1) + c : d / 2 * (Math.sqrt(1 - (b -= 2) * b) + 1) + c
    },
    easeInElastic: function (a, b, c, d, e) {
        var f = 1.70158,
			g = 0,
			h = d;
        if (0 == b) return c;
        if (1 == (b /= e)) return c + d;
        if (g || (g = .3 * e), h < Math.abs(d)) {
            h = d;
            var f = g / 4
        } else var f = g / (2 * Math.PI) * Math.asin(d / h);
        return -(h * Math.pow(2, 10 * (b -= 1)) * Math.sin(2 * (b * e - f) * Math.PI / g)) + c
    },
    easeOutElastic: function (a, b, c, d, e) {
        var f = 1.70158,
			g = 0,
			h = d;
        if (0 == b) return c;
        if (1 == (b /= e)) return c + d;
        if (g || (g = .3 * e), h < Math.abs(d)) {
            h = d;
            var f = g / 4
        } else var f = g / (2 * Math.PI) * Math.asin(d / h);
        return h * Math.pow(2, -10 * b) * Math.sin(2 * (b * e - f) * Math.PI / g) + d + c
    },
    easeInOutElastic: function (a, b, c, d, e) {
        var f = 1.70158,
			g = 0,
			h = d;
        if (0 == b) return c;
        if (2 == (b /= e / 2)) return c + d;
        if (g || (g = .3 * e * 1.5), h < Math.abs(d)) {
            h = d;
            var f = g / 4
        } else var f = g / (2 * Math.PI) * Math.asin(d / h);
        return 1 > b ? -.5 * h * Math.pow(2, 10 * (b -= 1)) * Math.sin(2 * (b * e - f) * Math.PI / g) + c : h * Math.pow(2, -10 * (b -= 1)) * Math.sin(2 * (b * e - f) * Math.PI / g) * .5 + d + c
    },
    easeInBack: function (a, b, c, d, e, f) {
        return void 0 == f && (f = 1.70158), d * (b /= e) * b * ((f + 1) * b - f) + c
    },
    easeOutBack: function (a, b, c, d, e, f) {
        return void 0 == f && (f = 1.70158), d * ((b = b / e - 1) * b * ((f + 1) * b + f) + 1) + c
    },
    easeInOutBack: function (a, b, c, d, e, f) {
        return void 0 == f && (f = 1.70158), (b /= e / 2) < 1 ? d / 2 * b * b * (((f *= 1.525) + 1) * b - f) + c : d / 2 * ((b -= 2) * b * (((f *= 1.525) + 1) * b + f) + 2) + c
    },
    easeInBounce: function (a, b, c, d, e) {
        return d - jQuery.easing.easeOutBounce(a, e - b, 0, d, e) + c
    },
    easeOutBounce: function (a, b, c, d, e) {
        return (b /= e) < 1 / 2.75 ? 7.5625 * d * b * b + c : 2 / 2.75 > b ? d * (7.5625 * (b -= 1.5 / 2.75) * b + .75) + c : 2.5 / 2.75 > b ? d * (7.5625 * (b -= 2.25 / 2.75) * b + .9375) + c : d * (7.5625 * (b -= 2.625 / 2.75) * b + .984375) + c
    },
    easeInOutBounce: function (a, b, c, d, e) {
        return e / 2 > b ? .5 * jQuery.easing.easeInBounce(a, 2 * b, 0, d, e) + c : .5 * jQuery.easing.easeOutBounce(a, 2 * b - e, 0, d, e) + .5 * d + c
    }
}),

$.support.cssProperty = function () {
    "use strict";
    return function (a, b) {
        var c, d, e = (document.body || document.documentElement).style;
        if ("undefined" == typeof e) return !1;
        if ("string" == typeof e[a]) return b ? a : !0;
        for (c = "Moz Webkit Khtml O ms Icab".split(" "), a = a.charAt(0).toUpperCase() + a.substr(1), d = 0; d < c.length; d++) if ("string" == typeof e[c[d] + a]) return b ? c[d] + a : !0
    }
}(),

function () {
    "use strict";
    var a, b = 0,
		c = ["ms", "moz", "webkit", "o"];
    for (a = 0; a < c.length && !window.requestAnimationFrame; ++a) window.requestAnimationFrame = window[c[a] + "RequestAnimationFrame"], window.cancelAnimationFrame = window[c[a] + "CancelAnimationFrame"] || window[c[a] + "CancelRequestAnimationFrame"];
    window.requestAnimationFrame || (window.requestAnimationFrame = function (a) {
        var c = (new Date).getTime(),
			d = Math.max(0, 16 - (c - b)),
			e = window.setTimeout(function () {
			    a(c + d)
			}, d);
        return b = c + d, e
    }), window.cancelAnimationFrame || (window.cancelAnimationFrame = function (a) {
        clearTimeout(a)
    })
}(),

function (a) {
    "use strict";
    a.fn.uncomment = function (b) {
        a(this).contents().each(function () {
            if (b && this.hasChildNodes()) a(this).uncomment(b);
            else if (8 == this.nodeType) {
                var c = a("<span>" + this.nodeValue + "</span>");
                a(this).replaceWith(c.contents())
            }
        })
    }
}(jQuery),

function (a, b, c) {
    "use strict";

    function d(b, c) {
        this.$o = a({}), this._name = e, this.element = b, this.$element = a(b), this.options = a.extend({}, a.fn[e].defaults, c), this.$trigger = this.$element.find("." + this.options.btnClass), this.$content = this.$element.find("." + this.options.contentClass), this.init()
    }
    var e = "dropdown",
		f = [],
		g = a.support.cssProperty("transition"),
		h = "transitionend otransitionend webkitTransitionEnd";

    d.prototype = {
        init: function () {
            this.addEvents(), this.options.closed === !1 && this.open()
        },
        open: function () {
            a.fn[e].closeAll(), this.isOpen = !0, this.$element.addClass("is-open"), this.$trigger.addClass("is-active"), this.publishEventOnTransitionEnd("/dropdown/open"), a(c.body).on("click.dropdown", a.proxy(this.close, this)), a(c).on("keydown.dropdown", a.proxy(this.closeByEsc, this))
        },
        close: function () {
            this.isOpen = !1, this.$element.removeClass("is-open"), this.$trigger.removeClass("is-active"), this.publishEventOnTransitionEnd("/dropdown/close"), a(c.body).off("click.dropdown"), a(c).off("keydown.dropdown")
        },
        closeByEsc: function (a) {
            27 === a.keyCode && this.close()
        },
        toggle: function (a) {
            this.isOpen ? this.close() : this.open(), a.stopPropagation(), a.preventDefault()
        },
        center: function () {
            if (this.options.centerContent) {
                var a = this.$trigger.outerWidth(),
					b = this.$content.outerWidth();
                this.$content.css("left", -(Math.abs(a - b) / 2))
            }
        },
        addEvents: function () {
            this.center(),
            this.$trigger.on("click.dropdown", a.proxy(this.toggle, this)),
            this.$content.on("click.dropdown", function (a) {
                a.stopPropagation()
            })
        },
        subscribe: function () {
            this.$o.on.apply(this.$o, arguments)
        },
        unsubscribe: function () {
            this.$o.off.apply(this.$o, arguments)
        },
        publish: function () {
            this.$o.trigger.apply(this.$o, arguments)
        },
        publishEventOnTransitionEnd: function (b) {
            g ? this.$content.one(h, a.proxy(this.publish, this, b)) : this.publish(b)
        }
    },
    a.fn[e] = function (b) {
        return this.each(function () {
            a.data(this, "plugin_" + e) || f.push(a.data(this, "plugin_" + e, new d(this, b)))
        })
    }, a.fn[e].defaults = {
        wrapClass: "Dropdown",
        btnClass: "Dropdown-control",
        contentClass: "Dropdown-view",
        centerContent: !1,
        closed: !0
    },
    a.fn[e].closeAll = function () {
        f.forEach(function (a) {
            a.isOpen && a.close()
        })
    }
}(jQuery, window, document),

function () {
    function a(a) {
        if (OldValidatorUpdateDisplay(a), "inline" === a.style.display || "visible" === a.style.visibility) {
            a.style.display = "block";
            var b = $(a).parent();
            b = b.hasClass("has-reveal") ? b.parent() : b, b.addClass(c)
        }
    }

    function b(a) {
        return $("." + c).removeClass(c), OldPage_ClientValidate(a)
    }
    if ("undefined" != typeof ValidatorUpdateDisplay) {
        if ($.isFunction(ValidatorUpdateDisplay)) {
            var c = "FailedInput";
            OldValidatorUpdateDisplay = ValidatorUpdateDisplay, ValidatorUpdateDisplay = a
        }
        $.isFunction(Page_ClientValidate) && (OldPage_ClientValidate = Page_ClientValidate, Page_ClientValidate = b, ValidatorOnChange = function (a) {
            a = a || window.event, Page_InvalidControlToBeFocused = null;
            var b;
            b = "undefined" != typeof a.srcElement && null != a.srcElement ? a.srcElement : a.target, $(b).closest("." + c).removeClass(c);
            var d;
            if ("undefined" != typeof b.Validators ? d = b.Validators : "label" == b.tagName.toLowerCase() && (b = document.getElementById(b.htmlFor), d = b.Validators), d) for (var e = 0; e < d.length; e++) ValidatorValidate(d[e], null, a);
            ValidatorUpdateIsValid()
        })
    }
}(),

function (a) {
    "use strict";
    var b = a(".has-reveal");
    b.length > 0 && b.each(function () {
        var b = a(this),
			c = a(document.createElement("span")),
			d = a(document.createElement("div")),
			e = b.find("input");
        d.addClass("reveal-pass u-dn").appendTo(b), c.addClass("reveal-eye").appendTo(b),
        c.on("mousedown", function () {
            d.html(e.val()).removeClass("u-dn")
        }),
        c.on("mouseup mouseleave", function () {
            d.addClass("u-dn")
        }),
        e.on("keyup", function () {
            e.val().length > 0 ? c.addClass("is-visible") : c.removeClass("is-visible")
        })
    })
}(jQuery, window),

function (a, b) {
    "use strict";

    function c() {
        return b.history.back(), !1
    }
    var d = a(".js-go-back");
    d.length < 1 || d.on("click.back", c)
}(jQuery, window),

function (a) {
    "use strict";

    function b(b, d) {
        this._name = c, this.element = b, this.$element = a(b), this.options = a.extend({}, a.fn[c].defaults, d), this.init()
    }
    var c = "fileupload";
    b.prototype = {
        init: function () {
            this.$inputElem = this.$element.find("." + this.options.inputClass), this.$valueElem = this.$element.find("." + this.options.inputValueClass), this.attachEvents()
        },
        onChangeHanlder: function () {
            this.$valueElem.text(this.$inputElem.val().replace("C:\\fakepath\\", ""))
        },
        attachEvents: function () {
            this.$inputElem.on("change", a.proxy(this.onChangeHanlder, this))
        }
    }, a.fn[c] = function (d) {
        return this.each(function () {
            a.data(this, "plugin_" + c) || a.data(this, "plugin_" + c, new b(this, d))
        })
    }, a.fn[c].defaults = {
        wrapperClass: "FileUpload",
        inputClass: "FileUpload-input",
        inputValueClass: "FileUpload-value"
    }
}(jQuery, window, document),

function (a, b, c) {
    "use strict";

    function d(b, c) {
        this.element = b, this.$element = a(b), this.options = a.extend({}, f, c), this._name = e, this.$element.each(a.proxy(this.setLinkParams, this)), this.$element.on("click", a.proxy(this.openPopup, this))
    }
    var e = "sharer",
		f = {
		    media: "twitter",
		    hashtag: !1,
		    url: !1,
		    text: !1,
		    popupWidth: 580,
		    popupHeight: 400
		};
    d.prototype = {
        setLinkParams: function () {
            var b = this.$element,
				d = "",
				e = b.parent().attr("id") ? "%23" + b.parent().attr("id") : "",
				f = this.options.url || b.data("url") || c.location.href.replace(c.location.hash, ""),
				g = this.options.hashtag || b.data("hashtag") || "",
				h = this.options.text || b.data("text") || b.text().trim() || a(c).find("title").text().trim();
            switch (this.options.media) {
                case "facebook":
                    d = "http://www.facebook.com/sharer.php?p[url]=" + encodeURI(f) + e + "&p[title]=" + encodeURI(h);
                    break;
                case "google":
                    d = "https://plus.google.com/share?url=" + encodeURI(f) + e;
                    break;
                case "linkedin":
                    d = "https://www.linkedin.com/shareArticle?mini=true&url=" + encodeURI(f) + e + "&title=" + encodeURI(h);
                    break;
                default:
                case "twitter":
                    d = "https://twitter.com/share?url=" + encodeURI(f) + e + "&text=" + encodeURI(h) + "&hashtags=" + g
            }
            b.attr("href", d)
        },
        openPopup: function (c) {
            c.preventDefault();
            var d = this.options.popupWidth,
				e = this.options.popupHeight,
				f = (a(b).width() - d) / 2,
				g = (a(b).height() - e) / 2,
				h = "status=1,width=" + d + ",height=" + e + ",top=" + g + ",left=" + f;
            b.open(c.target, "Share", h)
        }
    }, a.fn[e] = function (b) {
        return this.each(function () {
            a.data(this, "plugin_" + e) || a.data(this, "plugin_" + e, new d(this, b))
        })
    }
}(jQuery, window, document),

function (a, b) {
    "use strict";

    function c(c, f) {
        this.element = c,
        this.$element = a(c),
        this.options = a.extend({}, e, f),
        this._name = d,
        this.$window = a(b),
        this.working = !1,
        this.menuItems = this.$element.find("a"),
        "auto" === this.options.top && (this.options.top = this.$element.outerHeight()),
        this.menuItems.on("click.navspy", a.proxy(this.itemclick, this)),
        this.scrollItems = this.menuItems.map(function () {
            var b = a(this.getAttribute("href"));
            return b.length ? b : void 0
        }),
        this.$window.on("scroll.navspy", a.proxy(this.windowscroll, this))
    }
    var d = "navspy",
		e = {
		    top: "auto"
		};
    c.prototype = {
        itemclick: function (b) {
            var c, d = a(b.target.getAttribute("href"));
            b.preventDefault(), d[0] && (c = d.is(":visible") ? d.offset().top : d.closest(".Section").offset().top, a("html, body").stop().animate({
                scrollTop: c - this.options.top + 2
            }, 400))
        },
        windowscroll: function () {
            this.scroll = this.$window.scrollTop(), this.requestTick()
        },
        requestTick: function () {
            this.working || (b.requestAnimationFrame(a.proxy(this.check, this)), this.working = !0)
        },
        check: function () {
            var c = this.options.top,
				d = a(b).scrollTop() + c,
				e = this.scrollItems.map(function () {
				    return this.offset().top < d ? this : void 0
				});
            if (e = e[e.length - 1], e && e.length) {
                var f = e[0].id;
                this.menuItems.removeClass("is-active").filter("[href=#" + f + "]").addClass("is-active")
            } else this.menuItems.removeClass("is-active");
            this.working = !1
        }
    }, a.fn[d] = function (b) {
        return this.each(function () {
            a.data(this, "plugin_" + d) || a.data(this, "plugin_" + d, new c(this, b))
        })
    }
}(jQuery, window, document),

function (a, b) {
    "use strict";

    function c(c, f) {
        this.element = c, this.$element = a(c), this.options = a.extend({}, e, f), this._name = d, this.$window = a(b), this.position = this.$element.css("position"), this.fixed = !1, this.working = !1, this.width = this.$element.width(), this.height = this.$element.height(), this.offset = this.$element.offset(), this.$window.on("scroll.fixit", a.proxy(this.onscroll, this)).trigger("scroll.fixit")
    }
    var d = "fixit",
		e = {
		    top: 0,
		    fixClass: "is-fixed",
		    dummyClass: "fixed-dummy",
		    classOnly: !0,
		    renderDummy: !0
		};
    c.prototype = {
        onscroll: function () {
            this.scrollTop = this.$window.scrollTop(), this.requestTick()
        },
        requestTick: function (c) {
            c = c || "check", this.working || (b.requestAnimationFrame(a.proxy(this[c], this)), this.working = !0)
        },
        check: function () {
            this.scrollTop + parseInt(this.options.top, 10) > this.offset.top ? this.fixed || this.fix() : this.fixed && this.reset(), this.working = !1
        },
        addDummy: function () {
            this.$dummy = a("<div>", {
                "class": this.options.dummyClass
            }).insertBefore(this.$element)
        },
        fix: function () {
            this.options.renderDummy && this.addDummy(), this.$element.addClass(this.options.fixClass), this.options.classOnly || this.$element.css({
                width: this.width,
                height: this.height,
                position: "fixed",
                top: this.options.top + "px",
                left: this.offset.left
            }), this.fixed = !0
        },
        reset: function () {
            this.$element.removeClass(this.options.fixClass), this.options.classOnly || this.$element.css({
                position: this.position
            }), this.options.renderDummy && this.$dummy.remove(), this.fixed = !1
        }
    }, a.fn[d] = function (b) {
        return this.each(function () {
            a.data(this, "plugin_" + d) || a.data(this, "plugin_" + d, new c(this, b))
        })
    }
}(jQuery, window, document),

function (a) {
    "use strict";

    function b(b, e) {
        this.element = b, this.$element = a(b), this.options = a.extend({}, d, e), this._name = c, this.initialState = this.$element.hasClass(this.options.toggleClass), this.options.self ? this.$element.on("click", a.proxy(this.toggle, this)) : this.$element.on("click", "." + this.options.switcherClass, a.proxy(this.toggle, this)), this.$element.on("click", "." + this.options.resetClass, a.proxy(this.reset, this))
    }
    var c = "switcher",
		d = {
		    toggleClass: "is-toggled",
		    switcherClass: "Panel-switch",
		    resetClass: "Panel-reset",
		    self: !1,
		    group: !1,
		    preventDefault: !0,
		    stopPropagation: !1,
		    callback: null
		};
    b.prototype = {
        toggle: function (b) {
            var c, d = this.options,
				e = d.toggleClass;
            if (d.preventDefault && b.preventDefault(), d.stopPropagation && b.stopPropagation(), !a.isFunction(d.callback) || d.callback.call(this, this) !== !1) if (d.group) {
                if (c = a(b.target).closest(d.group), c.hasClass(d.toggleClass)) return;
                this.$element.find("." + e).removeClass(e), c.addClass(e)
            } else this.$element.toggleClass(d.toggleClass)
        },
        reset: function (a) {
            var b = this.options;
            b.preventDefault && a.preventDefault(), b.stopPropagation && a.stopPropagation(), b.group ? this.$element.find("." + b.toggleClass).removeClass(b.toggleClass) : this.$element.removeClass(b.toggleClass)
        }
    }, a.fn[c] = function (d) {
        return this.each(function () {
            a.data(this, "plugin_" + c) || a.data(this, "plugin_" + c, new b(this, d))
        })
    }
}(jQuery, window, document),

function (a, b, c) {
    "use strict";
    function d(c, d) {
        this.$element = a(c), this.tabLinks = this.$element.find("a"), this.options = a.extend({}, f, d), this.innerWrap = a(this.options.wrapperSelector), this.tabs = this.innerWrap.children(), this.tabsCount = this.tabs.length, this.activeTab = parseInt(this.options.activeTab, 10) || 1, this.automation = !1;
        var e = this;
        this.tabLinks.on("click", function (a) {
            "undefined" != typeof e.automation && e.automation !== !1 && e._AutomationPause(), e.show(a, null)
        }), this._hashcheck(), this.activeTab && this.activeTab > 0 && this.show(null, this.activeTab - 1), this.options.automated && this._autoRotate();
        var g = !1;
        a(b).on("resize", function () {
            g = !0
        }), b.setInterval(function () {
            g && (g = !1, e.innerWrap.height(e.tabs.filter(".is-active").height()))
        }, 800)
    }
    var e = "tlrkTabs",
		f = {
		    wrapperSelector: ".tlrk-tabs",
		    activeTab: 1,
		    scrollToTabs: !0,
		    automated: !1,
		    automatedTimer: 5e3,
		    automatedFreeze: !0,
		    setAnchors: !0,
		    animateHeight: !1,
		    preventDefault: !0,
		    stopPropagation: !1
		};
    d.prototype = {
        _AutomationPause: function () {
            this.automation && (b.clearInterval(this.automation), this.automation = !1)
        },
        _AutomationPlay: function () {
            this.automation || this._initAutomation()
        },
        _initAutomation: function () {
            this.automation && b.clearInterval(this.automation);
            var a = this;
            this.automation = b.setInterval(function () {
                a._showNextTab()
            }, this.options.automatedTimer)
        },
        _showNextTab: function () {
            var a = this.tabLinks.filter(".is-active").eq(0).index(),
				b = a === this.tabLinks.length - 1 ? 0 : a + 1;
            this.show(null, b)
        },
        _autoRotate: function () {
            this._initAutomation();
            var c = this;
            this.options.automatedFreeze && (this.innerWrap.on("mouseenter", function () {
                c._AutomationPause()
            }), this.innerWrap.on("mouseleave", function () {
                c._AutomationPlay()
            }));
            var d = !1,
				e = function () {
				    var d = c._calcPosition(),
						e = d[0],
						f = d[1],
						g = a(b).scrollTop(),
						h = g + a(b).height();
				    f > g && h > e ? c._AutomationPlay() : c._AutomationPause()
				};
            a(b).on("scroll", function () {
                d = !0
            }), b.setInterval(function () {
                d && (d = !1, e())
            }, 250)
        },
        _hashcheck: function () {
            var a = c.location.hash,
				d = this;
            a && (a = d.tabLinks.filter("a[href='" + a + "']").parent().index() + 1, d.activeTab = a >= 1 ? a : d.activeTab, b.setTimeout(function () {
                a >= 1 && d._scrollToItself()
            }, 250))
        },
        _historyEdit: function (a) {
            return ("undefined" == typeof a || null === a) && (a = ""), a.length < 1 ? void (b.history.pushState ? b.history.pushState("", c.title, " ") : b.location.hash = "") : (a = new RegExp("#").test(a) ? a : "#" + a, void (b.history.replaceState ? b.history.replaceState("", c.title, a) : b.location.replace(a)))
        },
        _calcPosition: function () {
            var a = this.innerWrap.closest(".Section").length > 0 ? this.innerWrap.closest(".Section") : this.innerWrap,
				b = a.offset().top;
            return [b, b + a.height()]
        },
        _scrollToItself: function () {
            var b = this._calcPosition(),
				c = b[0];
            a("html,body").animate({
                scrollTop: c
            }, 400)
        },
        show: function (b, c) {
            this.tabLinks.removeClass("is-active");
            var d = this.options,
				e = b ? a(b.target).addClass("is-active") : this.tabLinks.eq(c).addClass("is-active"),
				f = c || this.tabLinks.index(e);
            b && (d.preventDefault && b.preventDefault(), d.stopPropagation && b.stopPropagation(), d.scrollToTabs && this._scrollToItself(), d.setAnchors && this._historyEdit(e.attr("href"))), this.tabs.removeClass("is-active").eq(f).addClass("is-active"), d.animateHeight && this.innerWrap.animate({
                height: this.tabs.filter(".is-active").height()
            }, 350)
        }
    }, a.fn[e] = function (b) {
        return this.each(function () {
            a.data(this, "plugin_" + e) || a.data(this, "plugin_" + e, new d(this, b))
        })
    }
}(jQuery, window, document),

function (a, b, c, d) {
    "use strict";
    var e = a.support.cssProperty("transition"),
		f = function (c, d) {
		    var e, f, g, h, i, j = this;
		    this.elem = c, this.$elem = h = a(c), this.options = d, this.$slides = g = h.find(this.options.slides), this.current = 0, this.max = g.length + (d.seamless ? 1 : 0), this.activeDotClass = d.activeDotClass, this.itemWidth = 100 / this.max, d.nav && (this.nav = !0, this.$nav = d.generateNav ? a(this._generateNav(d.seamless ? this.max - 1 : this.max)).appendTo(h) : a(d.cssNav), this.$nav.find("a:first").addClass(this.activeDotClass).end().delegate("a", "click", function (b) {
		        b.preventDefault(), j.go(a(this).data("slide"))
		    })), d.prev && a(d.prev).bind("click", function () {
		        return j.prev(), !1
		    }), d.next && a(d.next).bind("click", function () {
		        return j.next(), !1
		    }), f = a("<div class='" + d.cssStrip + "'/>"), e = a("<div class='" + d.cssWrap + "'/>"), f.css({
		        width: 100 * this.max + "%",
		        position: "relative"
		    }), i = {
		        margin: 0,
		        display: "inline-block"
		    }, i.width = 100 / this.max + "%", g.css(i), g.appendTo(f), d.seamless && g.eq(0).clone().appendTo(f), f.appendTo(e), e.prependTo(h), this.$strip = f, this.$wrap = e, this._setSpeed(), d.play && (this.start(), d.stopOnHover && this.$elem.on("mouseenter", a.proxy(this.stop, this)).on("mouseleave", function () {
		        j.start()
		    })), "undefined" != typeof b.ontouchstart && (e[0].addEventListener("touchstart", a.proxy(this._touchstart, this)), e[0].addEventListener("touchend", a.proxy(this._touchend, this)), e[0].addEventListener("touchcancel", a.proxy(this._touchend, this)), e[0].addEventListener("touchmove", a.proxy(this._touchmove, this)))
		};
    f.prototype = {
        _translateX: function (a) {
            var b = this.$strip[0].style;
            b.MozTransform = b.webkitTransform = b.transform = b.msTransform = b.OTransform = "translateX(" + -a + "%)"
        },
        _setSpeed: function (a) {
            var b = this.$strip[0].style;
            return 0 === a ? (a = 0, void (b.webkitTransitionDuration = b.MozTransitionDuration = b.msTransitionDuration = b.OTransitionDuration = b.transitionDuration = "0ms")) : (a = a || this.options.speed, void (b.webkitTransitionDuration = b.MozTransitionDuration = b.msTransitionDuration = b.OTransitionDuration = b.transitionDuration = a + "ms"))
        },
        _resetPosition: function (a) {
            "undefined" == typeof a && (a = this.current), e ? this._translateX(a * this.itemWidth) : this.$strip.css("left", "-" + 100 * a + "%")
        },
        _setActiveNav: function (a) {
            "undefined" == typeof a && (a = this.current), this.$nav.find("." + this.activeDotClass).removeClass(this.activeDotClass).end().find("[data-slide='" + a + "']").addClass(this.activeDotClass)
        },
        go: function (c) {
            var d = this,
				f = this.options; -1 > c || c > this.max - 1 || c === this.current || f.seamless && this.isAnimated || (this.target = c, this._animationStart(), e ? b.setTimeout(function () {
				    d.$strip.one("transitionend otransitionend webkitTransitionEnd", a.proxy(d._animationEnd, d)), d._translateX(d.target * (100 / d.max))
				}, 4) : this.$strip.animate({
				    left: "-" + 100 * this.target + "%"
				}, f.speed, a.proxy(this._animationEnd, this)), this.current = this.target, this.nav && this._setActiveNav(f.seamless && this.current === this.max - 1 ? 0 : this.current))
        },
        next: function () {
            var a = this.current + 1;
            a > this.max - 1 && this.options.loop && (a = 0), this.go(Math.min(a, this.max - 1))
        },
        prev: function () {
            var a = this.current - 1;
            0 > a && this.options.loop && (a = this.max - 1), this.go(Math.max(a, 0))
        },
        start: function (c) {
            this.playing = !0, c && this.next(), this.timeout && b.clearTimeout(this.timeout), this.timeout = b.setTimeout(a.proxy(this.start, this, !0), this.options.playTimeout)
        },
        stop: function () {
            this.playing = !1, this.timeout && b.clearTimeout(this.timeout)
        },
        _touchstart: function (a) {
            this.scrolling = d, this._setSpeed(0), this.deltaX = 0, this.startX = a.touches[0].pageX, this.startY = a.touches[0].pageY, this.stripeWidth = this.$strip.width(), a.stopPropagation()
        },
        _touchend: function () {
            var a = this.stripeWidth / 1e3;
            this._setSpeed(), this.scrolling || (Math.abs(this.deltaX) > a ? this.deltaX < 0 && this.current > 0 ? this.prev() : this.deltaX > 0 && this.current < this.max - 1 ? this.next() : this._resetPosition() : this._resetPosition(), this.deltaX = null, this.startX = null, this.startY = null)
        },
        _touchmove: function (a) {
            var b = this.current * this.itemWidth;
            a.touches.length > 1 || a.scale && 1 !== a.scale || (this.deltaX = this.startX - a.touches[0].pageX, this.deltaX = this.deltaX / this.stripeWidth * 100, "undefined" == typeof this.scrolling && (this.scrolling = Math.abs(this.startX - a.touches[0].pageX) < Math.abs(a.touches[0].pageY - this.startY)), this.scrolling || (this._translateX(this.startX - a.touches[0].pageX < 0 && this.current > 0 || this.startX - a.touches[0].pageX > 0 && this.current < this.max - 1 ? b + this.deltaX : b + this.deltaX / 3.5), a.preventDefault()))
        },
        _generateNav: function (a) {
            var b, c = "<div class='" + this.options.cssNav + "'>";
            for (b = 0; a > b; b++) c += "<a data-slide='" + b + "' href='#'></a>";
            return c + "</div>"
        },
        _animationStart: function () {
            var c = this;
            this.$elem.addClass("in-motion"), this.isAnimated = !0, this.options.seamless && this.target === this.max - 1 && 0 === this.current && (this._setSpeed(0), this._resetPosition(this.max - 1), b.setTimeout(function () {
                c._setSpeed()
            }, 4), this.target = this.max - 2), a.isFunction(this.options.animationStart) && this.options.animationStart.call(this)
        },
        _animationEnd: function () {
            var c = this;
            this.$elem.removeClass("in-motion"), this.options.seamless && this.current === this.max - 1 ? (this._setSpeed(0), this.current = 0, this._resetPosition(), b.setTimeout(function () {
                c._setSpeed(), c.isAnimated = !1
            }, 50)) : this.isAnimated = !1, a.isFunction(this.options.animationEnd) && this.options.animationEnd.call(this)
        }
    }, a.fn.tlrkSlide = function (b) {
        return this.each(function () {
            var c = a(this),
				d = c.data("tlrkSlide"),
				e = a.extend({}, a.fn.tlrkSlide.defaults, "object" == typeof b && b),
				g = "string" == typeof b ? b : e.slide;
            d || c.data("tlrkSlide", d = new f(this, e)), "number" == typeof b ? d.go(b) : g && d[g]()
        })
    }, a.fn.tlrkSlide.defaults = {
        activeDotClass: "is-active",
        cssWrap: "ts-wrap",
        cssStrip: "ts-strip",
        cssNav: "ts-nav",
        slides: ".slide",
        loop: !1,
        play: !1,
        speed: 300,
        playTimeout: 5e3,
        stopOnHover: !0,
        generateNav: !1,
        nav: !1,
        seamless: !1
    }
}(jQuery, window, document),

function (a, b, c, d) {
    "use strict";

    function e(b) {
        var c = t.width(),
			d = t.height(),
			e = a(b);
        b.originalWidth > c ? e.css("max-width", .84 * c) : e.css("max-width", "none"), b.originalHeight > d - 60 ? e.css("max-height", .84 * d) : e.css("max-height", "none")
    }

    function f(a, b) {
        a.css({
            width: b.options.width,
            height: b.options.height
        })
    }

    function g(b) {
        var c = a.Deferred(),
			d = a("<img />", {
			    src: b.elem.href
			});
        return d.load(function () {
            (b.options.resize === !0 || 1 === Number(b.options.resize) || "yes" === b.options.resize) && (this.originalWidth = this.width, this.originalHeight = this.height, e(this)), c.resolve(this)
        }), f(d, b), c.promise()
    }

    function h(b) {
        var c, d = b.elem.href;
        return -1 !== d.indexOf("?") && -1 == d.indexOf("wmode=transparent") && (d += "&wmode=transparent"), -1 == d.indexOf("?") && -1 == d.indexOf("&") && -1 == d.indexOf("wmode=transparent") && (d += "?wmode=transparent"), c = a("<iframe></iframe>", {
            src: d,
            width: b.options.width,
            height: b.options.height,
            frameborder: 0,
            allowfullscreen: !0,
            mozallowfullscreen: !0,
            webkitallowfullscreen: !0
        })
    }

    function i(b) {
        var c = b.elem.href.split("#")[1];
        if (c) {
            var d = a("#" + c),
				e = d.find("iframe");
            return d[0] ? w[c] = b : d = w[c].cachedContent, e.length > 0 && e.each(function () {
                var a = this.src; -1 !== a.indexOf("?") && -1 == a.indexOf("wmode=transparent") && (a += "&wmode=transparent"), -1 == a.indexOf("?") && -1 == a.indexOf("&") && -1 == a.indexOf("wmode=transparent") && (a += "?wmode=transparent"), this.src !== a && (this.src = a)
            }), d.removeClass(b.options.csshide), f(d, b), d
        }
    }

    function j(b, c) {
        this.elem = b, this.$elem = a(b), this.timer = 0;
        var d, e = {};
        for (d in v) "undefined" != typeof this.$elem.data(d) && (e[v[d]] = this.$elem.data(d));
        this.options = a.extend({}, a.fn.litebox.defaults, c, e), this.init()
    }

    function k() {
        y && (b.clearTimeout(y), y = !1), t.width() >= z && !x && (a(".litebox").litebox(), x = !0), t.width() < z && (x = !1, a(".litebox[data-lite-info='content']").litebox())
    }

    function l(a, b, c) {
        a.addEventListener ? a.addEventListener(b, c) : a.attachEvent && a.attachEvent("on" + b, function () {
            c.call(a)
        })
    }
    var m, n, o, p, q, r = a.support.cssProperty("animation"),
		s = "webkitAnimationEnd mozAnimationEnd oanimationend animationend",
		t = a(b),
		u = a(c),
		v = {
		    "lite-info": "info",
		    "lite-width": "width",
		    "lite-height": "height",
		    "lite-resize": "resize"
		},
		w = {},
		x = !1;
    r === d && (r = !1), j.prototype = {
        init: function () {
            var b = this;
            b.$root = a(b.options.root || "body"), m || b.stage(), b.$elem.on("click", a.proxy(b.show, b))
        },
        stage: function () {
            var b = this;
            m = a("<div />", {
                id: "tlrk-overlay",
                "class": b.options.csshide
            }), n = a("<div />", {
                id: "tlrk-litebox",
                "class": b.options.csshide
            }), o = a("<div />", {
                "class": "litebox-content " + b.options.csshide
            }), p = a("<div />", {
                "class": "litebox-loader"
            }).html("Loading ..."), q = a("<a />", {
                href: "#",
                "class": "litebox-close"
            }).html("&times;"), o.on("click", ".litebox-close-btn", function (a) {
                a.preventDefault(), b.hide()
            }), b.$root.append(m, n), n.append(o, q)
        },
        populate: function () {
            var a = this;
            a.cachedContent === d ? (o.append(p), "image" === a.options.info ? g(a).done(function (b) {
                a.cachedContent = b, a.opened && (o.html(a.cachedContent), a.appendTitle(), a.center())
            }) : "video" === a.options.info ? (a.cachedContent = h(a), p.remove(), o.html(a.cachedContent), a.appendTitle()) : "content" === a.options.info && (a.cachedContent = i(a), p.remove(), o.html(a.cachedContent), a.appendTitle())) : ("image" === a.options.info && e(a.cachedContent), o.html(a.cachedContent), a.appendTitle()), a.center()
        },
        appendTitle: function () {
            var b = this;
            if (b.elem.title) {
                var c = a("<p />", {
                    "class": "litebox-title"
                });
                c.text(b.elem.title), o.append(c)
            }
        },
        destroy: function () {
            o.empty()
        },
        show: function () {
            var b = this;
            return b.opened = !0, b.options.noscroll && b.$root.addClass("tlrk-litebox-noscroll"), b.populate(), n.add(q).removeClass(b.options.csshide), r ? (m.removeClass(b.options.csshide).addClass(b.options.animIn), o.removeClass(b.options.csshide).addClass(b.options.animInSec)) : m.fadeIn("fast", function () {
                o.fadeIn()
            }), b.center(), m.add(q).add(n).one("click", a.proxy(b.hide, b)), o.on("click", function (a) {
                a.stopPropagation()
            }), u.on("keydown.litebox-keydown", function (c) {
                (27 === c.keyCode || 8 === c.keyCode && !a(c.target).is("input") && !a(c.target).is("textarea")) && (b.hide(), c.preventDefault())
            }), t.on("resize.litebox-resize", function () {
                b.timer && clearTimeout(b.timer), b.timer = setTimeout(a.proxy(b.resize, b), 150)
            }), x ? !1 : void 0
        },
        hide: function () {
            var a = this;
            return a.opened = !1, q.addClass(a.options.csshide), r ? (m.removeClass(a.options.animIn).addClass(a.options.animOut), o.removeClass(a.options.animInSec).addClass(a.options.animOutSec), o.one(s, function () {
                a.options.noscroll && a.$root.removeClass("tlrk-litebox-noscroll"), a.$root.addClass("chrome23-bugfix"), b.setTimeout(function () {
                    a.$root.removeClass("chrome23-bugfix")
                }, 100), m.removeClass(a.options.animOut).addClass(a.options.csshide), o.removeClass(a.options.animOutSec).addClass(a.options.csshide), n.addClass(a.options.csshide), a.destroy()
            })) : o.fadeOut("fast", function () {
                m.fadeOut(), n.addClass(a.options.csshide), a.destroy(), a.options.noscroll && a.$root.removeClass("tlrk-litebox-noscroll")
            }), u.off("keydown.litebox-keydown"), t.off("resize.litebox-resize"), !1
        },
        center: function () {
            o.css({
                "margin-top": -(o.height() / 2),
                "margin-left": -(o.width() / 2)
            })
        },
        resize: function () {
            var a = this;
            e(a.cachedContent), a.center()
        }
    }, a.fn.litebox = function (b) {
        return this.each(function () {
            a.data(this, "litebox") || a.data(this, "litebox", new j(this, b))
        })
    }, a.fn.litebox.defaults = {
        info: "image",
        animIn: "fadeIn",
        animOut: "fadeOut",
        animInSec: "fadeInScale",
        animOutSec: "fadeOutScale",
        csshide: "u-dn",
        resize: !0,
        noscroll: !1,
        root: "body>form"
    };
    var y = !1,
		z = 990;
    k(), l(b, "resize", function () {
        y || (y = b.setTimeout(k, 1e3))
    }), l(b, "orientationchange", k)
}(jQuery, window, document),

function (a, b, c) {
    "use strict";

    function d(c, d) {
        this.element = c, this.$element = a(c), this.options = a.extend({}, f, d), this._name = e, this.oldContent = "", this.$window = a(b), this.buttonTypes = {
            side: {
                "facebook-ext": "button_count",
                facebook: "bubble",
                twitter: "horizontal",
                googleplus: "medium",
                linkedin: "right"
            },
            top: {
                "facebook-ext": "box_count",
                facebook: "box_count",
                twitter: "vertical",
                googleplus: "tall",
                linkedin: "top"
            },
            none: {
                "facebook-ext": "button",
                facebook: "",
                twitter: "none",
                googleplus: "medium",
                linkedin: ""
            }
        }, this.oldContent = this.$element.html(), this.$element.html(""), this.runProfile()
    }
    var e = "tlrkShare",
		f = {
		    profile: "counter-side",
		    action: "replace",
		    profiles: {
		        "counter-side": {
		            "facebook-like": !1,
		            "facebook-share": !0,
		            twitter: !0,
		            linkedin: !0,
		            googleplus: !0,
		            mail: !0,
		            counter: "side"
		        },
		        "counter-top": {
		            "facebook-like": !1,
		            "facebook-share": !0,
		            twitter: !0,
		            linkedin: !0,
		            googleplus: !0,
		            mail: !1,
		            counter: "top"
		        },
		        basic: {
		            "facebook-like": !1,
		            "facebook-share": !0,
		            twitter: !0,
		            linkedin: !0,
		            googleplus: !0,
		            mail: !0,
		            counter: "none"
		        }
		    }
		};
    d.prototype = {
        runProfile: function () {
            this.loadSharrre(), this.getProfileSetting("facebook-share") && this.loadExtFacebook(), this.getProfileSetting("mail") && this.initMailTo()
        },
        loadSharrre: function () {
            var b = this;
            "function" != typeof a.fn.sharrre ? a.getScript("//cdn.jsdelivr.net/sharrre/1.3.5/jquery.sharrre.min.js", function () {
                b.initSharrre()
            }) : b.initSharrre()
        },
        loadExtFacebook: function () {
            a('<div id="fb-root" class="u-dn"></div>').appendTo(this.$element), function (a, b, c) {
                var d, e = a.getElementsByTagName(b)[0];
                a.getElementById(c) || (d = a.createElement(b), d.id = c, d.src = "//connect.facebook.net/en_US/sdk.js#xfbml=1&appId=0&version=v2.0", e.parentNode.insertBefore(d, e))
            }(c, "script", "facebook-jssdk"), this.initExtFacebook()
        },
        initSharrre: function () {
            this.$element.sharrre({
                share: {
                    facebook: this.getProfileSetting("facebook-like"),
                    twitter: this.getProfileSetting("twitter"),
                    linkedin: this.getProfileSetting("linkedin"),
                    googlePlus: this.getProfileSetting("googleplus")
                },
                buttons: {
                    googlePlus: {
                        size: this.getProfileButtonType("googleplus"),
                        annotation: "none" == this.getProfileSetting("counter") ? "none" : "bubble"
                    },
                    facebook: {
                        layout: this.getProfileButtonType("facebook")
                    },
                    twitter: {
                        count: this.getProfileButtonType("twitter")
                    },
                    linkedin: {
                        counter: this.getProfileButtonType("linkedin")
                    }
                },
                enableHover: !1,
                enableCounter: !1,
                enableTracking: "none" != this.getProfileSetting("counter")
            })
        },
        initExtFacebook: function () {
            this.$element.prepend('<div class="fb-share-button" data-href="' + c.URL + '" data-type="' + this.getProfileButtonType("facebook-ext") + '"></div>')
        },
        initMailTo: function () {
            this.$element.append('<a href="mailto:?subject=' + a("head title").text() + "&amp;body=" + c.URL + '" class="mailto js-mailto"></a>'), a(".js-mailto").on("click", function (b) {
                b.preventDefault(), c.location.href = a(this).attr("href")
            })
        },
        getProfileSetting: function (a) {
            return this.options.profiles[this.options.profile][a]
        },
        getProfileButtonType: function (a) {
            return this.buttonTypes[this.options.profiles[this.options.profile].counter][a]
        }
    }, a.fn[e] = function (b) {
        return this.each(function () {
            a.data(this, "plugin_" + e) || a.data(this, "plugin_" + e, new d(this, b))
        })
    }
}(jQuery, window, document),

function (a, b, c) {
    "use strict";

    function d(a, b) {
        function c(a) {
            return a.replace(k, function (a, b) {
                return b.toUpperCase()
            })
        }

        function d(a) {
            function b(a) {
                return !isNaN(parseFloat(a)) && isFinite(a)
            }
            return "false" === a.toLowerCase() ? !1 : "true" === a.toLowerCase() ? !0 : b(a) ? parseInt(a, 10) : a
        }
        var e, f, g, h, i = a.attributes,
			j = {},
			k = /-([\da-z])/gi;
        for (f = 0, e = i.length; e > f; f++) g = i[f].name, g.indexOf("data-" + b) > -1 && (h = c(i[f].name.replace("data-" + b + "-", "")), j[h] = d(i[f].value));
        return j
    }
    a(c).ready(function () {
        a("[data-tlrk-plugin]").each(function () {
            var b, c, e = a(this),
				f = this.getAttribute("data-tlrk-plugin").split(" "),
				g = f.length;
            for (c = 0; g > c; c++) b = f[c], a.isFunction(a.fn[b]) && e[b](d(this, b.toLowerCase()))
        })
    })
}(jQuery, window, document),

$(function () {
    "use strict";

    function a(a) {
        a.altKey && a.shiftKey && 70 === a.keyCode && (d.animate({
            scrollTop: 0
        }, "fast", "easeOutQuad").promise().done(function () {
            f.toggle(a)
        }), a.preventDefault())
    }

    function b() {
        e.trigger("focus")
    }

    function c() {
        e.trigger("blur")
    }
    var d = $("html, body"),
		e = $(".js-search-input"),
		f = $(".js-Bar-search").data("plugin_dropdown");
    f && ($(document).on("keydown.searchkey", a), f.subscribe("/dropdown/open", b), f.subscribe("/dropdown/close", c))
}),

$(function () {
    "use strict";

    function a() {
        c.prop("checked", !1)
    }
    var b = $("#js-Bar"),
		c = b.find("#Bar-controlMe"),
		d = b.find(".Dropdown");
    d.each(function (b, c) {
        var d = $(c).data("plugin_dropdown");
        "undefined" != typeof d && d.subscribe("/dropdown/open", a)
    })
}),

function (a, b, c) {
    "use strict";

    function d(a) {
        a = a || "", a.length > 0 && e(i.find("[data-id='" + a.replace("#", "") + "']"))
    }

    function e(a) {
        var b = a.prev().length > 0 ? a.prev() : j.last(),
			c = a.next().length > 0 ? a.next() : j.first(),
			d = {
			    prev: b.data("id"),
			    next: c.data("id")
			};
        n.add(p).attr("href", "#" + d.next), m.attr("href", "#" + d.prev), q.text(c.find(".js-feature-headline").text()), j.add(g).removeClass("is-visible"), a.add(i).addClass("is-visible"), t.push({
            event: "virtualEvent",
            eventCategory: "product-features",
            eventAction: "text",
            eventLabel: k + "/" + a.data("id")
        }), f()
    }

    function f() {
        u && 8 == u && m.add(n).add(o).add(p).each(function (b, c) {
            var d = c.className;
            a(c).removeClass(d).addClass(d)
        })
    }
    if (!(a("#Features").length < 1)) {
        var g = a(".Features-grid"),
			h = g.find(".js-open-feature"),
			i = a(".Features-items"),
			j = i.find(".FeatureDetails-contentItem"),
			k = b.location.pathname.replace("products/", "").replace("/overview.aspx", "").replace("/", "").replace(".aspx", ""),
			l = a(".FeatureDetails-controls"),
			m = l.find("a[data-action='prev']"),
			n = l.find("a[data-action='next']"),
			o = l.find("a[data-action='all']"),
			p = i.find(".FeatureDetails-contentNext"),
			q = p.find(".next-item-title"),
			r = b.location.hash.split("?")[0] || "",
			s = [],
			t = b.dataLayer || [],
			u = c.documentMode || !1;
        if (a.each(h, function (b, c) {
			var d = a(c).attr("href") || "";
			s.push(d)
        }),
            a(b).on("hashchange", function (c) {
			var e = c.originalEvent.oldURL || "",
				f = e.split("#")[1],
				h = b.location.hash,
				k = -1 !== a.inArray(h, s) ? !0 : !1;
			"" !== h && k ? d(h) : (j.add(i).removeClass("is-visible"), g.addClass("is-visible")), f || a("body,html").not(":animated").animate({
            scrollTop: g.offset().top - 155
        }, 400)
        }),
            o.on("click", function (d) {
			if (d.preventDefault(), j.add(i).removeClass("is-visible"), g.addClass("is-visible"), t.push({
				event: "virtualEvent",
            eventCategory: "product-features",
            eventAction: "text",
            eventLabel: k + "/all"
        }), b.history.pushState && !u) b.history.pushState("", c.title, b.location.pathname);
        else {
				f();
				var e = a(b).scrollTop();
				b.location.hash = "", a(b).scrollTop(e)
        }
        }),
            r.length > 0 && -1 !== a.inArray(r, s)) {
            t.push({
                event: "virtualEvent",
                eventCategory: "product-features",
                eventAction: "text",
                eventLabel: r
            }), d(r);
            var v = i.offset();
            a(b).scrollTop(v.top - 155)
        }
    }
}(jQuery, window, document);